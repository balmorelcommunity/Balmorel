LOOP(Y$(YVALUE(Y) LE YVALUE(IYALIAS)),HEATSV_VHS.FX(Y,IA,HEATSV_STEPS) = HEATSV_VHS.L(Y,IA,HEATSV_STEPS););

$ifi %INDHEATSAV%==yes           LOOP(Y$(YVALUE(Y) LE YVALUE(IYALIAS)),HEATSV_VINDHEATHS.FX(Y,INDHEAT_IA,HEATSV_STEPS) = HEATSV_VINDHEATHS.L(Y,INDHEAT_IA,HEATSV_STEPS););

*Fixing the accumulated investments from previous optimization for the next optimization
HEATSV_VHSACCUMNET.FX(IY411FIRST,IA,HEATSV_STEPS)$HEATSV_POT(IA,HEATSV_STEPS)
= HEATSV_VHSACCUMNET.L(IY411FIRST,IA,HEATSV_STEPS)
;

$ifi %INDHEATSAV%==yes         HEATSV_VINDHEATHSACCUMNET.FX(IY411FIRST,INDHEAT_IA,HEATSV_STEPS)$HEATSV_INDHEATPOT(INDHEAT_IA,HEATSV_STEPS)
$ifi %INDHEATSAV%==yes         = HEATSV_VINDHEATHSACCUMNET.L(IY411FIRST,INDHEAT_IA,HEATSV_STEPS)
$ifi %INDHEATSAV%==yes         ;

