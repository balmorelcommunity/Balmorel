+SUM((IR,IA,HEATSV_STEPS)$(RRRAAA(IR,IA) AND HEATSV_POT(Y,IA,HEATSV_STEPS)), HEATSV_VHS(Y,IA,HEATSV_STEPS)*HEATSV_COST(Y,IR,HEATSV_STEPS) )
+SUM((IYALIAS2,IR,IA,HEATSV_STEPS)$(RRRAAA(IR,IA) AND ORD(IYALIAS2) LT ORD(Y) AND HEATSV_POT(Y,IA,HEATSV_STEPS)), HEATSV_VHS(IYALIAS2,IA,HEATSV_STEPS)*HEATSV_COST(Y,IR,HEATSV_STEPS) )

$ifi not %INDHEATSAV%==yes $goto endINDHEAT
+SUM((IR,INDHEAT_IA,INDHEAT_GROUP,HEATSV_STEPS)$(RRRAAA(IR,INDHEAT_IA) AND HEATSV_INDHEATPOT(Y,INDHEAT_IA,HEATSV_STEPS)), HEATSV_VINDHEATHS(Y,INDHEAT_IA,HEATSV_STEPS)*HEATSV_COST(Y,IR,HEATSV_STEPS) )
+SUM((IYALIAS2,IR,INDHEAT_IA,INDHEAT_GROUP,HEATSV_STEPS)$(RRRAAA(IR,INDHEAT_IA) AND ORD(IYALIAS2) LT ORD(Y) AND HEATSV_INDHEATPOT(Y,INDHEAT_IA,HEATSV_STEPS)), HEATSV_VINDHEATHS(Y,INDHEAT_IA,HEATSV_STEPS)*HEATSV_COST(Y,IR,HEATSV_STEPS) )
$label endINDHEAT
